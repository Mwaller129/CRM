<%- include('../partials/header') %>
<h2>Activites</h2>
<table id="list">
    <thead>
        <tr>
            <th>Upcoming Activites</th> 
        </tr>
    </thead>
    <tbody>
        <% dashboards.forEach(function(a) { %>
            <tr>
                <td><%= a.activies %></td>
                <td><a href="/dashboards/<%= a._id %>">DETAILS</a></td>
            </tr>
        <% }) %>
    </tbody>
</table>
<%- include('../partials/footer') %>